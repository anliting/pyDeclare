pyDeclare is a Python library for building declarative stateful systems. Instead of manually maintaining state, we describe states and the rules governing transitions between them, and the framework applies those rules systematically.

## Install

`pip install git+https://github.com/anliting/pyDeclare.git`

